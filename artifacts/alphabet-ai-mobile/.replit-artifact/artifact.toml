kind = "mobile"
previewPath = "/mobile/"
title = "Alphabet AI Mobile"
version = "1.0.0"
id = "artifacts/alphabet-ai-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
name = "expo"
paths = [ "/mobile/" ]
localPort = 8081

[services.development]
run = "pnpm --filter @workspace/alphabet-ai-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alphabet-ai-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/alphabet-ai-mobile", "run", "serve" ]

[services.env]
PORT = "8081"
BASE_PATH = "/mobile/"
